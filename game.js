import { Ship, Gameboard, Player } from "./classes";

class Gameplay {
  constructor() {
    this.gameOver = false;
    this.playerOnTurn = this.playerOnTurn;
  }

  player = new Player("real", "Player");
  computer = new Player("computer", "Computer");

  //display one grid per player on the html screen
  displayBoard() {
    document.querySelector(".game-grid");
    for()
    document.createElement(div)
  }
}
